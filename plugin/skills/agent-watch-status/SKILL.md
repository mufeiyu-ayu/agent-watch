---
name: agent-watch-status
description: Inspect Agent Watch configuration, installation, enabled state, and mute status without exposing secrets.
---

Run `agent-watch status` and report the result. Never print the raw config file or unmasked credentials.
