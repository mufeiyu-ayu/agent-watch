---
description: Show Agent Watch configuration and notification status
---

Run `agent-watch status` with the Bash tool. Return the command result. Never reveal an unmasked Webhook or signing secret.
