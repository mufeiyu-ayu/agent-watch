---
description: Switch Agent Watch notification template
argument-hint: <compact|standard|detailed>
---

Run `agent-watch template "$ARGUMENTS"` with the Bash tool. If the preset is missing, ask the user to choose compact, standard, or detailed. Return the command result.
